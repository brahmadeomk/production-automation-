# Core runtime
PyYAML>=5.4

# Touchscreen GUI (SRS section 12).
# On Raspberry Pi OS install the distribution package instead:
#     sudo apt install python3-pyqt5
PyQt5>=5.15

# XLSX export (SRS section 14)
openpyxl>=3.0

# GPIO driver for Raspberry Pi OS Trixie (SRS section 5).
# Not installable off-Pi; the station falls back to a simulated backend.
lgpio>=0.2; platform_machine == "aarch64" or platform_machine == "armv7l"
